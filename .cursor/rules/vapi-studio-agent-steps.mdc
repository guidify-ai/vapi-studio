---
description: Vapi Studio agent-step conventions — intention names and lifecycle method order
globs: "**/conversation/nodes/**/*.ts,src/node/**/*.ts,projects/**/config/flow.yaml,projects/**/config/poc/*.brain.yml"
alwaysApply: false
---

# Vapi Studio agent-step conventions

## Intention naming

- **Actions**: `is{VerbInPast}…` — e.g. `isCollectedFirstName`, `isAcceptedAppointment`
- **Polarity**: `studio.isPositive`, `studio.isNegative`
- **Multi-choice**: soft affirmatives that do not name an option → `resolveIntention` re-ask which lane; never Brain/unknown

## Brain (ChatGPT adapter)

- Live calls MUST be fast: scan abort ≤ 3s; target first speech < 1.5s.
- Cheap model is set in `VapiStudioModule.forRoot({ brain: { model } })`, not `.env`.
- Scan candidates = listen intentions + portal intentions + `studio.isUnknownTransition`.

## TypeScript style (framework + apps)

- **Explicit access modifiers** on every class member: `public`, `protected`, or `private` — no default (implicit public).
- **Typed properties** — declare types on class fields (`public phase: IntentionCascadePhase = …`), not untyped inference.
- Enforced in the framework via `yarn lint` (`@typescript-eslint/explicit-member-accessibility`, `@typescript-eslint/typedef`).

## Lifecycle method order

`before()` → `listen()` → `run()` → `after()` → `catch()`

| Method | Put here | Not here |
| --- | --- | --- |
| `before()` | CAN gate; async prep / JWT or integration **warm** (spawn without await when speech must not wait) | Spoken copy |
| `listen()` | Intentions / extract / hints | I/O |
| `run()` | Speech + one terminal output | Auth/CRM/teardown that can live in before/after |
| `after()` | Non-speech teardown | TTS / terminal output |
| `catch()` | Recovery only when overridden | Happy-path side effects |

Do **not** overload `run()` with integrations — prefer `before()` / `after()`. Detached warms: hold `Promise` on a Nest service keyed by conversation id; later Nodes `await` it.

```ts
export class ExampleStep extends AgentNode<Schema> {
  public async before(ctx): Promise<boolean> { /* gate + spawn warm */ return true; }
  public async listen(ctx): Promise<ListenExpectation | null> { /* … */ }
  public async run(ctx): Promise<NodeResult> { /* speak + terminal only */ }
  public async after(ctx, result): Promise<void> { /* teardown */ }
}
```

Full rules: `docs/guides/node-conventions.md` and `docs/best-practices/nodes-and-listens.md`.
