# Workspace legacy

Historical SpecKit specs, MVP initiation doc, and zip artifacts from the pre–`vapi-studio` monorepo layout. Not part of the shipped framework.

Current layout: framework at repo root, applications under `projects/`.
